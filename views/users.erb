<h2>Registered with us</h2>
<p>Sorted alphabetically by name</p>

<ul>
  <% @users.each do |user| %>
    <li>

      <a href="/profile/<%= user["username"] %>"><%= user["full_name"] %> || <%= user["username"] %></a>
    
    </li>

  <% end %>
</ul>